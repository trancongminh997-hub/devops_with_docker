With given `docker-compose.yaml`, it works simply by running 
```
docker-compose up
```
Naviaget to `http://localhost:8080/`, the response similar to
```
{"message":"You connected to the following path: /","path":"/"}
```